package hello;

public class BattleResolution (GameArea attackingArea, int numberOfAttackers, GameArea defendingArea) {
    attackingArea.setStr(String.valueOf(parseInt(attackingArea.getStr) - numberOfAttackers));
    int new numberOfDefenders = parseInt(defendingArea.getStr);
    int new a = Math.random();

// Defender won:
//defendingArea.setStr(String.valueOf(numberOfDefenders));
        }

